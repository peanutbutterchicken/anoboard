name: deploy website on push
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    name: deploy
    runs-on: ubuntu-latest
    steps:
      - name: get latest code
        uses: actions/checkout@v6

      - name: sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_HOSTNAME_MAIN }}
          username: ${{ secrets.FTP_USERNAME_MAIN }}
          password: ${{ secrets.FTP_PASSWORD_MAIN }}
          protocol: ftp
          port: 21
          server-dir: /htdocs/
          exclude: |
            **/.git*
            **/.git*/**
            README.md
            config.database_config.example.php
            .env.example
